<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 64 64" width="64" height="64">
  <!-- Bold magenta "o" — approximates the pink/magenta lowercase-o
       OnPod mark. Outer ring is the brand color; transparent center
       so the dark UI shows through (matches the look of the screenshot
       the user shared). -->
  <circle
    cx="32"
    cy="32"
    r="22"
    fill="none"
    stroke="#d946ef"
    stroke-width="14"
  />
</svg>
